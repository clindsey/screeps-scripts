const harvesterStates = require('states.harvester');
const fsm = require('fsm');
const StateMachine = requie('StatemMachine');

class HarvesterEntity extends StateMachine {
  canCarryEnergy () {
  }

  spawnNeedsEnergy () {
  }

  inTransferRange () {
  }

  setDestinationSpawn () {
  }

  inCollectRange () {
  }

  setDestinationEnergy () {
  }

  harvest () {
  }

  needsToTransfer () {
  }

  transfer () {
  }

  distanceToDestination () {
  }

  followNav () {
  }
}
